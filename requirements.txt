# Python 3.10+
#
# Version floors, not pins: these are the lowest releases the notebooks were
# exercised against. Pin exactly (==) if you need byte-reproducible output.

# Spectral pipeline (notebooks 01, 02)
numpy>=1.26
scipy>=1.11
pandas>=2.1
astropy>=5.3
matplotlib>=3.8
requests>=2.31

# Regridding (notebook 03) uses scipy.spatial.cKDTree - no extra dependency

# Interactive viewer (notebook 04)
plotly>=5.18
Pillow>=10.0

# Static overlays (notebook 05)
opencv-python>=4.8
rasterio>=1.3

# Notebook environment
jupyterlab>=4.0
ipykernel>=6.25
